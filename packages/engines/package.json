{
  "name": "@mergesafe/engines",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "test": "vitest run --passWithNoTests",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@mergesafe/core": "workspace:*",
    "@mergesafe/rules": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.3.5"
  },
  "main": "src/index.ts",
  "types": "src/index.ts"
}
